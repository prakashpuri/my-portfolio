name: Cache Dependencies

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Cache dependencies
        id: cache-step
        uses: actions/cache@v4
        with:
          path: ~/.cache
          key: cache-c1304d4

      - name: prime-cache-c1304d4
        run: |
          echo "Cache hit: ${{ steps.cache-step.outputs.cache-hit }}"
```

---

**Commit karo → Action automatically trigger hoga!**

Phir Actions tab pe check karo green ✅ aaya ki nahi, aur repo URL submit karo:
```
https://github.com/prakashpuri/my-portfolio
